# Niffler Test Environment Configuration
# Frontend URL
FRONTEND_URL=http://frontend.niffler.dc:9000/

# Gateway URL (API Gateway)
GATEWAY_URL=http://gateway.niffler.dc:9000/

# Auth Service URL
AUTH_URL=http://auth.niffler.dc:9000/register

# Auth Secret (JWT secret key)
AUTH_SECRET=secret

# Database URLs
SPEND_DB_URL=postgresql+psycopg2://postgres:secret@gateway.niffler.dc:5432/niffler-spend
USERDATA_DB_URL=postgresql+psycopg2://postgres:secret@gateway.niffler.dc:5432/niffler-userdata

# Test User Credentials
TEST_USERNAME=usertest
TEST_PASSWORD=Password123!

# Kafka Configuration
KAFKA_ADDRESS=kafka:9092

# gRPC Service Host
GRPC_SERVICE_HOST=currency.niffler.dc:8092

# SOAP Service Address
SOAP_ADDRESS=http://userdata.niffler.dc:8089/ws
SOAP_ADDRESS=http://userdata.niffler.dc:8089/ws